name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install ruff black pytest
      - name: Lint
        run: |
          python -m ruff check .
      - name: Format check
        run: |
          python -m black --check .
      - name: Smoke compile
        run: |
          python -m py_compile (Get-ChildItem -Filter *.py | ForEach-Object FullName) (Get-ChildItem -Path utils -Filter *.py | ForEach-Object FullName)
      - name: Tests
        env:
          MPLBACKEND: Agg
        run: |
          python -m pytest

